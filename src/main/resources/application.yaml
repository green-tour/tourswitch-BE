server:
  port: 8088
  # 혼잡도 지도 응답은 폴리곤까지 담겨 490KB에 이른다. gzip을 켜면 115KB로 줄어든다(실측).
  # 홈에서도 지도를 그리므로 모바일에서 체감이 크다.
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/plain,text/css,text/javascript
    min-response-size: 2KB

spring:
  config:
    import: optional:file:./.env[.properties]

  application:
    name: tourswitch
    version: v1

  threads:
    virtual:
      enabled: true

  web:
    error:
      include-stacktrace: always
      include-message: always

  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  security:
    oauth2:
      client:
        registration:
          kakao:
            provider: kakao
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            # FE(Vercel)가 /api를 이 서버로 프록시하므로 카카오 콜백도 FE 도메인으로 받아야
            # 인가 요청 세션과 리프레시 쿠키가 같은 사이트에 유지된다. 앞단 Gateway가
            # X-Forwarded-Host를 덮어써 {baseUrl}로는 FE 도메인을 알 수 없어 환경변수로 지정한다.
            redirect-uri: ${OAUTH2_REDIRECT_URI:{baseUrl}/api/auth/oauth2/callback/{registrationId}}
            scope:
              - profile_nickname
              - profile_image
            client-name: Kakao

        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration-seconds: 3600
  refresh-token-expiration-seconds: ${JWT_REFRESH_TOKEN_EXPIRATION_SECONDS}

refresh-token:
  cookie:
    name: refreshToken
    secure: ${REFRESH_COOKIE_SECURE}
    same-site: Lax
    path: /api/auth

frontend:
  callback-uri: ${FRONTEND_CALLBACK_URI}
  origin: ${FRONTEND_ORIGIN}

tourapi:
  base-url: https://apis.data.go.kr/B551011
  connect-timeout-ms: 3000
  read-timeout-ms: 5000
  # 키를 쉼표로 여러 개 넣으면 앞의 키가 한도에 걸릴 때 다음 키로 넘어간다.
  kor-service:
    service-keys: ${TOUR_API_KEYS:${TOUR_API_KEY}}
  tats-cnctr-rate:
    service-keys: ${TOUR_API_KEYS:${TOUR_API_KEY}}

seoul-open-api:
  base-url: ${SEOUL_OPEN_API_BASE_URL:http://openapi.seoul.go.kr:8088}
  service-name: citydata
  service-key: ${SEOUL_OPEN_API_KEY}
  connect-timeout-ms: 3000
  read-timeout-ms: 5000
  collection-enabled: ${SEOUL_CROWD_COLLECTION_ENABLED:true}
  collection-initial-delay-ms: ${SEOUL_CROWD_COLLECTION_INITIAL_DELAY_MS:10000}
  collection-delay-ms: ${SEOUL_CROWD_COLLECTION_DELAY_MS:600000}

# 자치구별 관광지 목록 캐시. 한 번 갱신에 areaBasedList2 100회(25구 x 유형 4개)와
# 집중률 25회를 사용하므로 TourAPI 일일 한도에 맞춰 ttl-minutes를 조정한다.
# 파드마다 캐시를 따로 들고 있어 실제 호출량은 레플리카 수만큼 곱해진다.
#
# 기본값 720(12시간)은 개발계정 1,000회 기준이다. 레플리카 2개에서 하루 400회를 쓰고
# 나머지는 방 생성과 상세 조회 몫으로 남는다.
# 운영계정 10만회 승인 후에는 PLACE_CACHE_TTL_MINUTES=30으로 올린다(레플리카 2개 기준 하루 9,600회).
place-cache:
  enabled: ${PLACE_CACHE_ENABLED:true}
  ttl-minutes: ${PLACE_CACHE_TTL_MINUTES:720}
  initial-delay-minutes: ${PLACE_CACHE_INITIAL_DELAY_MINUTES:1}
